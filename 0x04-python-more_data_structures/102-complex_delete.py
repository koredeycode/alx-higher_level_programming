#!/usr/bin/python3
def complex_delete(a_dictionary, value):
    lk = list(a_dictionary)

    for i in lk:
        if value = a_dictionary.get(i):
            del a_dictionary[i]
    return (a_dictionary)
